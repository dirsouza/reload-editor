{
  "name": "reload-editor",
  "displayName": "Reload Editor",
  "description": "Add reload button to status bar for quick window reload",
  "version": "0.0.1",
  "publisher": "dirsouza",
  "icon": "./images/reload-icon.png",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dirsouza/reload-editor.git"
  },
  "homepage": "https://github.com/dirsouza/reload-editor",
  "bugs": {
    "url": "https://github.com/dirsouza/reload-editor/issues"
  },
  "activationEvents": [
    "*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "reload-plugin.reloadWindow",
        "title": "Reload Window"
      }
    ]
  },
  "scripts": {
    "build": "rimraf dist && rimraf *.vsix && yarn compile && yarn package && vsce package --no-dependencies --skip-license --allow-star-activation",
    "vscode:prepublish": "yarn run package",
    "compile": "yarn run check-types && yarn run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "yarn run check-types && yarn run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "yarn run compile-tests && yarn run compile && yarn run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "@types/vscode": "^1.30.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "esbuild": "^0.25.8",
    "eslint": "^9.32.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "6.0.1",
    "typescript": "^5.9.2"
  }
}